name: s_expendable_menu
description: A beautiful, customizable expandable menu widget for Flutter with smooth animations and multiple expansion directions
issue_tracker: https://github.com/SoundSliced/s_expendable_menu/issues
repository: https://github.com/SoundSliced/s_expendable_menu
version: 3.1.1
homepage: https://github.com/SoundSliced/s_expendable_menu

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  s_packages: ^3.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
